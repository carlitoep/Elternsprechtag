# Offizielles Java 17 Image
FROM eclipse-temurin:17-jdk

WORKDIR /app

# Gradle Wrapper & Projekt kopieren
COPY . .

# Build
RUN ./gradlew build --no-daemon

# Port freigeben
EXPOSE 8080

# Startbefehl
CMD ["java", "-jar", "build/libs/demo-0.0.1-SNAPSHOT.jar"]
